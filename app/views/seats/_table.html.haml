%table.table.table-bordered
    %thead
        %tr
            %th= 'Seat code'
            %th= 'Department'
            %th
    %tbody
        - @seats.each do |seat|
            %tr
                %td= seat.name
                %td= seat.department.name if seat.department_id.present?
                %td
                    %a{ href: seat_path(seat), target:'_blank' }= 'Detail'
                %td
                    %a{ href: seat_path(seat), data: { method: :delete }}= 'Delete'
                %td
                    %a{ href: edit_seat_path(seat)}= 'Edit'