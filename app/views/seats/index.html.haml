%h1.table_headline= t('seats.index.list_of_seats')

- if can? :create, Seat
  .row
    %div{class: 'float-right'}
      %a.btn.index_button_new.mb-3{ href: new_seat_path}= t('seats.index.new_seat_button')

= render'search'

= render'table'
